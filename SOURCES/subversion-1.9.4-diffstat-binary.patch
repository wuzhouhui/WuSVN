Index: subversion-1.9.4/subversion/libsvn_diff/diff.h
===================================================================
--- subversion-1.9.4.orig/subversion/libsvn_diff/diff.h
+++ subversion-1.9.4/subversion/libsvn_diff/diff.h
@@ -63,6 +63,7 @@ struct svn_dfstat_ctx_s {
   void *next;
   char *file_path;
   int base;
+  enum svn_dfstat_cmt cmt;
   svn_linenum_t inserted_num;
   svn_linenum_t deleted_num;
 };
Index: subversion-1.9.4/subversion/libsvn_diff/diff.c
===================================================================
--- subversion-1.9.4.orig/subversion/libsvn_diff/diff.c
+++ subversion-1.9.4/subversion/libsvn_diff/diff.c
@@ -772,7 +772,10 @@ show_data(DATA * p)
 	    printf("%*ld ", number_len, DelOf(p));
 	}
 	putchar('|');
-        plot_numbers(p);
+	if (p->cmt == svn_dfstat_bin)
+		printf("binary");
+	else
+		plot_numbers(p);
 	printf("\n");
     }
 }
@@ -996,10 +999,12 @@ svn_diff_destroy_dfctx(svn_dfstat_ctx_t 
 svn_error_t *
 svn_diff_stat(svn_dfstat_ctx_t *head,
     const svn_diff_t *diff,
-    const char *file_path)
+    const char *file_path,
+    enum svn_dfstat_cmt cmt)
 {
   svn_dfstat_ctx_t *t = apr_pcalloc(dfctx_pool, sizeof(*t));
   t->file_path = apr_pstrdup(dfctx_pool, file_path);
+  t->cmt = cmt;
   t->next = head->next;
   head->next = (void *)t;
   while (diff)
Index: subversion-1.9.4/subversion/include/svn_diff.h
===================================================================
--- subversion-1.9.4.orig/subversion/include/svn_diff.h
+++ subversion-1.9.4/subversion/include/svn_diff.h
@@ -1302,10 +1302,16 @@ svn_diff_create_dfctx(svn_dfstat_ctx_t *
 void
 svn_diff_destroy_dfctx(svn_dfstat_ctx_t *ctx);
 
+enum svn_dfstat_cmt {
+	svn_dfstat_normal = 0,
+	svn_dfstat_bin,
+};
+
 svn_error_t *
 svn_diff_stat(svn_dfstat_ctx_t *ctx,
     const svn_diff_t *diff,
-    const char *file_path);
+    const char *file_path,
+    enum svn_dfstat_cmt cmt);
 
 svn_error_t *
 svn_diff_output_dfstat(svn_stream_t *out,
Index: subversion-1.9.4/subversion/libsvn_client/diff.c
===================================================================
--- subversion-1.9.4.orig/subversion/libsvn_client/diff.c
+++ subversion-1.9.4/subversion/libsvn_client/diff.c
@@ -800,7 +800,7 @@ diff_content_changed(svn_boolean_t *wrot
   else if (! dwi->force_binary && (mt1_binary || mt2_binary) && dwi->diffstat)
     {
       /* Suppress binary files' warning */
-      SVN_ERR(svn_diff_stat(dwi->dfstat_ctx, NULL, diff_relpath));
+      SVN_ERR(svn_diff_stat(dwi->dfstat_ctx, NULL, diff_relpath, svn_dfstat_bin));
       return SVN_NO_ERROR;
     }
 
@@ -886,7 +886,7 @@ diff_content_changed(svn_boolean_t *wrot
             dwi->options.for_internal,
             scratch_pool));
       if (svn_diff_contains_diffs(diff))
-        SVN_ERR(svn_diff_stat(dwi->dfstat_ctx, diff, diff_relpath));
+        SVN_ERR(svn_diff_stat(dwi->dfstat_ctx, diff, diff_relpath, 0));
     }
   else   /* use libsvn_diff to generate the diff  */
     {
